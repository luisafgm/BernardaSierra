### BernardaSierra.com

requerido:
- node
- yarn
- npm
- serve (paquete npm)

# Comandos

iniciar página:
`yarn run start`

compilar página estática:
`yarn run build`

servir página estática:
`yarn run seve-build`